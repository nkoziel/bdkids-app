/*
 * Wiring and boot only — same rule as rayon-app's src/main.js. Real modules
 * (core/data/ui) land once the metadata source decision in the vault is settled.
 */
document.getElementById('results').textContent = 'Bibliothèque vide pour le moment.';
